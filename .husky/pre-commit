#!/usr/bin/env sh
set -e

. "$(dirname -- "$0")/_/husky.sh"

# Block commits that introduce known-sensitive values (AWS account IDs
# next to AWS resources, AKIA access keys, secret-key assignments).
# Bypass per-line: append `# allow-secret-scan`.
./scripts/check-secrets.sh

npx lint-staged
