---
'@hyperdx/app': patch
---

Run is now the primary button on Explore and Save view the secondary one. Run
previously dropped to secondary whenever the form was clean, which left the
toolbar with Save view as its only emphasised action — pointing at saving a
query rather than running one.
