---
'@hyperdx/app': patch
---

Metrics: the "browse metrics" control now sits inside the metric name field
instead of floating beside it, matching the query bar where the filter action is
part of the field frame. The two read as one control rather than a select and an
unrelated icon button.
