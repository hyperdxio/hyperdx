---
'@hyperdx/app': patch
'@hyperdx/common-utils': patch
---

fix: Show distribution only labels a value "<1%" when the sample shows it is rare, not when it has no matching rows, and shows 100% when the field's filter allows only that value
