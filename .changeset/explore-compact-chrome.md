---
'@hyperdx/app': patch
---

Compact the Explore page chrome so results start higher up the screen. "Add
filter" now sits inside the search field, the example filter chips, helper
paragraph, in-field "Search" badge, and the duplicate "Show SQL" toggle (still
available under the results overflow menu) are gone, and Columns / Sort moved
into the results row. The view switcher labels the view you are on — "Events",
"Chart as Time series" — and leaves the rest as icons.
