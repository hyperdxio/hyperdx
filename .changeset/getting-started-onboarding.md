---
"@hyperdx/app": minor
---

Add `GettingStarted` onboarding component with custom stepper, credentials table, and data sources configuration. Includes new `Sources` folder with refactored `SourcesList` and `SourceForm` components, custom Mantine button variants (primary, secondary, danger), and ErrorBoundary stories.

