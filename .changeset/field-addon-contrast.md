---
'@hyperdx/app': patch
---

Give input addons their own shade in both themes. Light mode inverts the old
relationship — the field is now the brightest surface and anything attached to
it (addons, hover) steps down into grey — and dark mode drops the addon below
the border instead of matching it, so an addon reads as part of the input's
frame rather than a button sitting inside it.
