---
'@hyperdx/app': minor
---

Explore chart types are collapsed into a single Charts segment and an "As <chart type>" menu beside it, so Events stays the primary results view. That menu appears only while a chart is on screen, since choosing a chart type next to a table of events changes nothing you can see.
