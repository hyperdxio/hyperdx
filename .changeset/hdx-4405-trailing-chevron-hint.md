---
"@hyperdx/app": patch
---

Dashboard table tiles configured with a row-click action now show the action hint as a trailing chevron icon at the right edge of each row, revealed on hover, with a small tooltip that names the destination. The icon click navigates to the same URL as a row click, with all the standard native browser behaviors (cmd-click new tab, middle-click new tab, right-click context menu).
