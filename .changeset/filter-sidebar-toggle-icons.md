---
'@hyperdx/app': patch
---

The filter sidebar's show/hide buttons use the sidebar glyphs the app already
uses for its other panel toggles rather than generic arrow bars. "Hide filters"
changes on both Search and Explore because the sidebar is shared between them;
"Show filters" changes on Explore.
