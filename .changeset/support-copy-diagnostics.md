---
'@hyperdx/app': patch
---

"Copy diagnostics" in the help menu (previously "Copy debug info") now includes
the last 20 failed requests and queries, and says when no session replay is
recording, so a support ticket needs a single paste.
