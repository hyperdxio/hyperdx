---
'@hyperdx/app': patch
---

Show password requirements on the Join Team page. When a user accepts a team invite and sets their password, the same live password policy checklist used on the auth/register page is now displayed, so users no longer have to guess the required length, casing, number, and special-character rules.
