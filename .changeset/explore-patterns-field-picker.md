---
'@hyperdx/app': patch
---

Explore: Event patterns pick a field the way Group by does (`on Body`) instead of asking for a SQL expression. Clicking a pattern can open matching events in the list, counts are labeled as estimates, and the view can be added to a dashboard. Varying tokens (`<*>`) in a pattern are highlighted so the stable shape can be read.
