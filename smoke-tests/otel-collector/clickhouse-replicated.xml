<?xml version="1.0"?>
<!--
  config.d overlay for the ch-server-replicated smoke-test service.

  The Replicated (DatabaseReplicated) database engine stores table metadata in
  Keeper, so this overlay enables the embedded single-node ClickHouse Keeper
  and points the server at it. The {shard}/{replica} macros are required by
  the Replicated database/table engine defaults used by the collector's
  schema seed.
-->
<clickhouse>
    <keeper_server>
        <tcp_port>9181</tcp_port>
        <server_id>1</server_id>
        <log_storage_path>/var/lib/clickhouse/coordination/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse/coordination/snapshots</snapshot_storage_path>
        <coordination_settings>
            <operation_timeout_ms>10000</operation_timeout_ms>
            <session_timeout_ms>30000</session_timeout_ms>
        </coordination_settings>
        <raft_configuration>
            <server>
                <id>1</id>
                <hostname>localhost</hostname>
                <port>9234</port>
            </server>
        </raft_configuration>
    </keeper_server>

    <zookeeper>
        <node>
            <host>localhost</host>
            <port>9181</port>
        </node>
    </zookeeper>

    <macros>
        <shard>s1</shard>
        <replica>r1</replica>
    </macros>

    <!-- The shared local config.xml hardcodes ch-server; point interserver
         replication at this service instead. -->
    <interserver_http_host>ch-server-replicated</interserver_http_host>
</clickhouse>
